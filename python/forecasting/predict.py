# load a joblib model and predict — stub
def run_forecast(product_id, start_date, end_date):
    # TODO: load model, create DF, predict, return list of {date, predicted}
    return [{"date":start_date, "predicted": 5}]
